[project]
name = "resnet-inference"
version = "0.1.0"
description = "ResNet inference and grid-search demo"
authors = ["AutoGenerated"]
requires-python = ">=3.12"
dependencies = [
  "torch>=1.12.0",
  "torchvision>=0.13.0",
  "toml>=0.10.2",
  "numpy<2"
]